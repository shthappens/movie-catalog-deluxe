<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Movie Catalog Deluxe</title>
  </head>

  <body>
    <h3>Actors</h3>

    <ul>
      <% @actors.each do |actor| %>
        <li><a href="/actors/<%= actor["id"] %>"><%= actor["name"] %></li>
      <% end %>
    </ul>

  </body>
</html>
